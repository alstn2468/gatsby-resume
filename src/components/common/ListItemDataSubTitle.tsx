
import { rem } from 'polished';
import { styled } from '~/src/components/themeContext';

const ListItemDataSubTitle = styled.i({
  fontSize: rem(16),
  color: '#3E424B',
});

export default ListItemDataSubTitle;